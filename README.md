# fedora-atomic-provisioning

## Quickstart

```bash
git clone --recurse-submodules https://github.com/kai-bruell/fedora-atomic-provisioning
cd fedora-atomic-provisioning
./apply.sh
```

> Set `TESTMODE=true` in `apply.sh` to do a dry run.
